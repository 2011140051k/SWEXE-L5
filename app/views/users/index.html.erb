<% @users.each do |t| %>
    <p>
        <%= t.uid %>
        <%= t.pass %>
        <%= link_to '削除', user_path(t), method: :delete %>
    </p>
<% end %>

<p>
    <%= link_to '新規ユーザー追加', new_user_path %>
</p>

<p>
    <%= link_to 'ツイート一覧', tweets_path %>
</p>